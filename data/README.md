# Data

Run `python src/collect_data.py` to collect public market data and create:

- `kospi200_all_data.csv`
- `kospi200_model_variables.csv`

The generated CSV files are ignored by Git by default if you add them to `.gitignore` for a fully reproducible repository.
